# Quantum Optics Simulator - System Dependencies
# Note: This is a hybrid React + WebAssembly project, not a Python project. 
# Therefore, dependencies are system-level and NPM-level, rather than PyPI packages.

# --- Core Tools ---
Node.js >= 18.0.0
npm >= 9.0.0

# --- WebAssembly Build Tools ---
CMake >= 3.14
Emscripten SDK (emsdk) >= 3.0.0
C++17 Compatible Compiler (GCC/Clang/MSVC)

# --- Frontend Package Dependencies (Installed via `npm install`) ---
# (See package.json for exact version constraints)
react
react-dom
three
zustand
gsap
vite
@vitejs/plugin-react
vite-plugin-glsl
